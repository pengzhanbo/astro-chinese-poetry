---
import BookCover from '~/components/BookCover.astro'
import Breadcrumb from '~/components/Breadcrumb.astro'
import PostContent from '~/components/PostContent.astro'
import { books, zhongYong } from '~/config/books'
import { Books } from '~/config/site'
import Layout from '~/layouts/Layout.astro'

const { title, abstract } = books[Books.zhongyong]
const { title: postTitle, paragraphs } = zhongYong.traditional
---

<Layout title={title}>
  <div class="py-6 px-5 md:px-10">
    <Breadcrumb list={[{ text: title, active: true }]} />
    <div
      class="xl:flex py-6 max-w-3xl mx-auto border-b border-b-border items-start"
    >
      <BookCover {title} class="mt-5 mx-auto" />
      <div class="flex-1 ml-4">
        {abstract.map((desc) => <p class="px-4">{desc}</p>)}
      </div>
    </div>
    <div class="pt-6 max-w-3xl mx-auto">
      <h3 class="text-2xl font-bold text-brand mt-10 mb-4 text-center">
        {postTitle}
      </h3>
      <PostContent content={paragraphs} indent />
    </div>
  </div>
</Layout>
